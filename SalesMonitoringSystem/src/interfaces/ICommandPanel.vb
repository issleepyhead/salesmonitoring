Public Interface ICommandPanel

    ''' <summary>
    ''' Serves as a delete command to tables
    ''' </summary>
    Sub Delete()

    ''' <summary>
    ''' Serves as an update command to tables
    ''' </summary>
    Sub Update()

    ''' <summary>
    ''' Serves as an add command to tables
    ''' </summary>
    Sub Add()
End Interface
